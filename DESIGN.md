# DESIGN.md — 设计思路

本文档说明 Scientific Learning Skills 的设计理念、认知科学依据和架构决策。

---

## 问题：为什么现有的 AI 学习助手不够好

三个通病：

| 通病 | 表现 | 为什么是问题 | 本项目的解法 |
|------|------|------------|------------|
| **百科式回答** | 问一个概念，返回维基百科式的长篇定义、历史背景、衍生概念 | 学习者不需要一本教材的压缩版，需要的是一个"入口" | 每个 Skill 有"输入判断"环节，先诊断阶段再决定输出深度和内容 |
| **没有诊断** | 假设用户需要从头学，但其实只是某一步卡住了 | 花 10 分钟讲用户已经懂的内容，真正卡住的地方 30 秒带过 | `fuzzy-understanding` 专门做 6 类卡点诊断 + 针对性修复 |
| **给答案不给方法** | 解题时直接给完整解答，没有思路推演 | 这道题看懂了，换一道还是不会 | `problem-solving` 分步推演，每一步说明"为什么选这个方法" + 方法总结 + 变式 |

**根本原因**：通用 AI 助手的默认行为是"给你所有相关信息"，但学习是一个**状态依赖**的过程——你现在在哪、卡在哪、想往哪走，决定了什么信息对你是最有价值的。

---

## 核心设计原则

### 1. 诊断先于讲解

在任何解释之前，必须先判断学习者当前处于什么阶段、卡在哪里。

```
不同阶段的学习者问同一句话，答案完全不同：

"什么是极限？"
  ├─ 零基础高中生 → 需要：走路趋近墙壁的直觉类比，暂不引入 ε-δ
  ├─ 大一新生学过但没懂 → 需要：诊断"符号不懂"还是"不知道为什么要有这定义"
  └─ 已掌握想深化 → 需要：极限的完备性视角、和高数其他概念的逻辑关系
```

### 2. 直觉先于定义

人对事物的理解从直觉开始，然后才形式化。这不是"降低标准"，而是符合认知规律。

认知负荷理论指出：当抽象定义和形式符号在没有直觉锚点的情况下呈现时，学习者的工作记忆超载，信息无法进入长时记忆。

**例子**：
- 极限的直觉：走向墙壁，每次走一半距离——"无限接近但不到达"
- 导数的直觉：车速表——你在某个瞬间的速度
- 行列式的直觉：矩阵对空间拉伸的体积倍率

### 3. 最小可运行

每个 Skill 都提供"最小例题"——数字最干净、步骤最少、只涉及当前概念的例子。复杂例子里学习者会迷失在细节中。

### 4. 理解通过迁移验证

"我懂了"可能是假的。只有能解决变式题（改了条件或场景的题）才算真正理解。

变式理论（Variation Theory）：学习者必须体验概念在不同情境下的表现，才能分辨核心特征和表面特征。

### 5. 错误是信息源

错误不是学习的失败，而是校准理解的最佳材料。"为什么我会这样错"比"正确答案是什么"更有教学价值。

这个原则直接塑造了 `mistake-review` 的设计：输入要求包含"你的错误解答"和"你当时怎么想的"，而不只是正确答案。

### 6. 一个 Skill 只解决一个问题

不要试图用一个通用 Skill 覆盖所有场景。零基础入门和诊断模糊理解的对话策略完全不同，深化和解题更是不同的问题。一个 Skill 把一件事做好，多个 Skill 通过转交规则协作。

### 7. 面向具体人群

这个项目默认面向**高中到本科低年级的理工科学生**。这意味着：
- 不假设研究者级别的数学成熟度
- 不用更高阶知识解释当前知识（如用实分析解释微积分）
- 使用他们已有的知识体系（高中数理基础）作为桥梁

---

## 认知科学依据

### 认知负荷理论

工作记忆容量有限。设计选择：
- 直觉先于定义 → 降低内在认知负荷
- 最小例题 → 降低外在认知负荷
- 分步推演 → 避免信息过载

### 建构主义

新知识必须挂靠到已有认知结构上。设计选择：
- 诊断前置缺失（哪个前置环节断了）
- 联系已有知识（"这个和你知道的 X 有什么关系"）
- 多视角解释（同一个概念挂靠到不同认知框架上）

### 提取练习效应

反复阅读不如主动回忆。设计选择：
- 自测题替代总结
- 填空/问答检测替代重读（`text-memorizer`）
- 间隔复习替代集中突击（`word-deep-dive`）

### 间隔效应

分散在时间上的学习比集中学习更有效。设计选择：
- 所有内容记忆型 Skill 使用统一的间隔复习引擎
- 初始 1 天，正确翻倍（1→2→4→8→16→32），错误重置为 1 天

### 变式理论

必须体验概念的关键特征在变化中保持不变。设计选择：
- 每个 Skill 强制要求变式训练
- 反例辨析是解释的固定环节
- 深化学习强调"多视角看同一概念"

---

## 学习进阶模型

```
零基础 ──→ 模糊理解 ──→ 清晰理解 ──→ 熟练应用 ──→ 深刻理解
  │            │            │            │            │
zero-base   fuzzy-       problem-     变式        deepening
-learning   understanding solving      训练        -learning
  │            │                         │
  │         mistake-review ←─────────────┘
  │            │
  └────── study-plan-builder（元认知层，管理"怎么学"）
```

不同阶段需要**完全不同的对话策略**。一个好的学习助手不是在每个阶段给相同的回答，而是先判断你在哪，再决定说什么。

### 六层解释框架

所有 Skill 共享一个解释框架：

```
直觉解释 → 正式定义 → 最小例题 → 变式迁移 → 反例辨析 → 常见误区
```

| 层次 | 解决的问题 | 缺失的后果 |
|------|-----------|-----------|
| 直觉解释 | "这东西在解决什么问题？" | 符号和公式没有意义 |
| 正式定义 | "精确来说它是什么？" | 理解模糊，不能严谨使用 |
| 最小例题 | "最简单的用法是什么？" | 理论落不了地 |
| 变式迁移 | "换了条件还能用吗？" | 换一道题就不会 |
| 反例辨析 | "不满足条件会怎样？" | 不知道定理的边界 |
| 常见误区 | "最容易犯什么错？" | 同样的坑反复踩 |

---

## Skill 设计模式

每个 SKILL.md 遵循统一的结构模式：

```yaml
---
name: skill-name
description: 何时触发的一句话
---

# 目标           — 这个 Skill 要解决什么问题
# 适用场景        — 什么时候用
# Memory 系统     — 存什么、存在哪、何时读写
# 输入判断        — 打开后先做什么判断
# 执行流程        — 步骤链
# 输出格式        — 输出结构模板
# 反例             — 什么时候不要这样做
# 测试样例        — 具体输入 + 期望输出方向
```

这个结构本身是一种约束：强制 Skill 作者思考诊断策略、边界条件和验证方式。

---

## 跨平台架构

Skills 本质上是**结构化的 Markdown 指令集**（每个 SKILL.md 约 2-5K tokens），不依赖任何平台特定的 API。部署时合成为系统提示注入目标智能体。

```
skills/                     # 规范源（平台无关）
  ├── zero-base-learning/SKILL.md
  ├── fuzzy-understanding/SKILL.md
  ├── ...
  └── text-memorizer/SKILL.md

RULES.md                    # 总规则（平台无关）
memory/
  └── review-engine.md      # 共享复习引擎（平台无关）

deploy/                     # 平台适配层
  ├── claude-code.md        # → .claude/skills/ + CLAUDE.md
  ├── codex.md              # → system message 注入
  ├── openclaw.md           # → Skills 目录或系统提示
  └── generic.md            # → 任意智能体的通用部署方法
```

Memory 系统（间隔复习、薄弱点追踪）依赖文件系统读写。在无文件系统访问的平台上（如 ChatGPT），可通过 Function Calling 连接外部存储，或直接禁用 Memory 功能。

## 架构决策

### Skill 选择：隐式触发而非手动切换

用户不需要打 `/零基础学习` 这样的命令，自然描述问题本身就是选择。决策树在 `CLAUDE.md` 中定义，根据关键词和描述特征自动路由。

### Skill 转交：对话中的协作

一个 Skill 在执行中发现更适合另一个 Skill 时，主动建议转移并说明原因。例如 `problem-solving` 解题时发现用户概念理解有误 → 转 `fuzzy-understanding` 先修复概念。

### Memory：自主写入而非询问

每个 Skill 自主决定何时写入、读取 Memory。不在对话中打断用户问"要不要存这个"——减少交互摩擦。内容记忆遵循统一的复习引擎，分析记忆仅作为上下文参考。

### 质量保障：eval 驱动迭代

每个 Skill 有对应的测试用例（`evals/test_cases.md`），10 维度评分标准（`evals/scoring_rubric.md`），8 种典型失败模式（`evals/failure_cases.md`）。实际测试 → 打分 → 改 Skill → 再测试，形成迭代闭环。

---

## 什么不是这个项目

- **不是教材替代品**：不提供系统性的课程内容，而是对话式的学习引导
- **不是刷题工具**：不追求题量，追求每道题的方法论价值
- **不是"AI 老师"的人格化尝试**：没有虚拟角色，对话风格直接简洁
- **不是通用问答的包装**：每个 Skill 是一个方法论引擎，不是 prompt 模板库

---

## 延伸阅读

- [CLAUDE.md](./CLAUDE.md) — 总规则层：决策树、转交规则、Memory 架构
- [docs/learning-methodology.md](./docs/learning-methodology.md) — 学习方法论详解
- [docs/design-principles.md](./docs/design-principles.md) — 早期设计原则记录
- [docs/roadmap.md](./docs/roadmap.md) — 版本路线图
